
При записі образу в базу даних перевірити чи не дублюються інградієти

При записі образу в базу даних перевірити чи не дублюються поля характеристик

При записі в базу удаляти всі пробіли в назві і перевіряти назву на валідність (хз???)

Журнал бази даних
   тобто помилки
   добавлення, видалення, редагування

   пожливість передивлятися журнал


CREATE TABLE `image`.`rtyr` (
  `t` INTEGER UNSIGNED NOT NULL AUTO_INCREMENT,
  `er` VARCHAR(45) NOT NULL DEFAULT '',
  PRIMARY KEY(`t`)
)
ENGINE = InnoDB;

